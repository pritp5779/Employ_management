// /**
//  * API_BASE_URL is already set to your live Hostinger API.
//  */
// const API_BASE_URL = 'https://fourpirates.space/empmanagment/api.php';



/**
 * API_BASE_URL — the live Hostinger API endpoint.
 * Both the local (Live Server) and deployed frontends talk to this same
 * backend, so this URL must match wherever api.php actually lives.
 */
const API_BASE_URL = 'https://fourpirates.space/empmanagment/api.php';